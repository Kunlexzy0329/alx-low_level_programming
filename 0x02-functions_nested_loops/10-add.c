#include "main.h"
/**
 * add - function that adds two integers and return the result
 * @i: parameters to be added
 * @j: parameter to be added
 * Return: sum of the two parameter
 *
 */

int add(int i, int j)
{
	int sum = 0;

	sum = i + j;
	return (sum);
}
