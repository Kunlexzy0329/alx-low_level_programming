#include "main.h"
/**
 *main - start point and it accepted two argument
 *@argc: the argument counter
 *@argv: the argument vector
 *Return: 0 always
 */

int main(int argc, char* argv[])
{
	printf("%s\n",argv[0]);
	return (0);
}
