#include "main.h"
/**
 *main - start of the program
 *@argc: the argument count
 *@argv: argument vector
 *Return: 0 always
 */

int main(int argc, char *argv[])
{
	if (argc > 1)
		printf("%d\n", argc);
	return (0);
}
