#include "main.h"
/**
 * mul - function that multiplies two integers
 * @a: parameter to be multiplied
 * @b: parameter to be multiplied with
 * Return: integer mul
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}
